import { ToggleThemeButton } from "./Header/ToggleThemeButton"

export const Header = () => {
    return <>
        <header>

            <div>Header</div>
            <ToggleThemeButton/>
        </header>
    </>
}
